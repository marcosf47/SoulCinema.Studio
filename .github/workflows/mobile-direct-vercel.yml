name: SoulCinema Direct Vercel

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/mobile-direct-vercel.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply RC430
        run: python3 .github/scripts/rc430.py
      - name: Verify RC430 mobile isolation
        run: grep -q 'RC430 MOBILE-ONLY LIVE FULLSCREEN RECOVERY' public/index.html && grep -q 'rc430-mobile-only-live-fullscreen-recovery' public/index.html && ! grep -q 'rc429-live-fullscreen-user-activation-bridge' public/index.html && grep -q 'rc400-master-mobile-live-fullscreen-authority' public/index.html && grep -q "matchMedia('(pointer:coarse)')" public/index.html && grep -q 'RC425 MOBILE THEATER STAGE GUARD' public/index.html && grep -q 'RC424 DISTINCT FEATURE FILM HANDOFF' public/index.html && grep -q 'RC418 THEATER FULLSCREEN EXIT ISOLATION' public/index.html && grep -q 'RC417 NATIVE THEATER FULLSCREEN ONLY' public/index.html && grep -q 'RC414 MOBILE CINEMATIC PORTALS REBUILD' public/index.html
      - name: Commit source
        run: git config user.name 'SoulCinema Direct Deploy' && git config user.email 'actions@users.noreply.github.com' && git add public/index.html && git commit -m 'RC430 mobile-only Live fullscreen recovery' && git push origin HEAD:main
