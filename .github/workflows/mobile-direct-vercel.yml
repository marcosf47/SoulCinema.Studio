name: SoulCinema Direct Vercel

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/mobile-direct-vercel.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Apply RC306 desktop Live Center viewport fit
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('public/index.html')
          html=p.read_text(encoding='utf-8')
          marker='<!-- RC306 DESKTOP LIVE CENTER VIEWPORT FIT -->'
          anchor='<!-- RC305 DESKTOP LIVE CENTER IFRAME STATE AUTHORITY -->'
          patch='''<!-- RC306 DESKTOP LIVE CENTER VIEWPORT FIT -->
<style id="rc306-desktop-live-viewport-fit">
@media(min-width:861px){
  #liveCenter.active{height:100dvh!important;max-height:100dvh!important;overflow:hidden!important;padding-bottom:12px!important;box-sizing:border-box!important}
  #liveCenter.active #livePlayerWrap{height:calc(100dvh - 250px)!important;min-height:360px!important;max-height:calc(100dvh - 250px)!important;overflow:hidden!important;box-sizing:border-box!important}
  #liveCenter.active #rc275LiveFrame{position:absolute!important;inset:0!important;width:100%!important;height:100%!important;max-width:100%!important;max-height:100%!important;border:0!important;object-fit:contain!important}
}
</style>
'''
          if marker not in html:
              if html.count(anchor)!=1:
                  raise SystemExit(f'RC305 anchor count={html.count(anchor)}; refusing to guess')
              html=html.replace(anchor,patch+anchor,1)
              p.write_text(html,encoding='utf-8')
          PY

      - name: Verify RC306 and preserve locks
        run: |
          grep -q 'RC306 DESKTOP LIVE CENTER VIEWPORT FIT' public/index.html
          grep -q 'RC305 DESKTOP LIVE CENTER IFRAME STATE AUTHORITY' public/index.html
          grep -q 'RC304 DESKTOP LIVE CENTER RECOVERY OWNER' public/index.html
          grep -q 'RC303 DESKTOP LIVE CENTER LUXURY + ALWAYS FULLSCREEN' public/index.html
          grep -q 'RC302 DESKTOP LIVE CENTER FULLSCREEN BUTTON FORCE VISIBLE' public/index.html
          grep -q 'RC301 DESKTOP LIVE CENTER REMOVE LOWER LIVE BADGE' public/index.html
          grep -q 'RC300 DESKTOP LIVE CENTER FIT + NATIVE FULLSCREEN RESTORE' public/index.html
          grep -q 'RC299 DESKTOP LIVE CENTER LUXURY LARGER SCREEN' public/index.html
          grep -q 'RC298 DESKTOP LIVE CENTER FULLSCREEN CONTROL' public/index.html
          grep -q 'RC297 DESKTOP PRODUCTION ROOM VIEWPORT FIT' public/index.html
          grep -q 'RC277 DESKTOP LIVE CENTER RETURN' public/index.html
          grep -q 'RC275 SOULCINEMA LIVE CENTER VPS EMBED RECOVERY' public/index.html
          grep -q 'RC271 DESKTOP LIVE CENTER EXIT MEDIA STOP' public/index.html
          grep -q 'RC293 MOBILE SKIP IMMEDIATE PORTAL OWNER' public/index.html
          grep -q 'rc258-mobile-screening-theater-restore' public/index.html

      - name: Commit RC306 source
        run: |
          git config user.name "SoulCinema Direct Deploy"
          git config user.email "actions@users.noreply.github.com"
          if git diff --quiet -- public/index.html; then
            echo "RC306 already applied"
          else
            git add public/index.html
            git commit -m "RC306 desktop Live Center viewport fit"
            git push origin HEAD:main
          fi
