{
  "name": "soulcinema-studio",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "app:add:android": "npx cap add android",
    "app:sync": "npx cap sync android",
    "app:open": "npx cap open android",
    "app:build": "npm run app:sync && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "next": "16.2.10",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  }
}
